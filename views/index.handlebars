<h1>Burger App!</h1>

<ul>
  {{!-- {{#each burgers}} --}}
  <li>
    <p class="burger-container">
      {{!-- {{this.id}}. {{this.burger_name}} --}}

      <button data-devoured-id="{{this.id}}" class="deldevoured">Devour Burger!</button>
    </p>
  </li>
  {{!-- {{/each}} --}}
</ul>

<h2>List a Burger</h2>
<form id="createburger_name" class="button-size">
  <textarea type="text" name="burger_name" class="new_item"></textarea>
  <button type="submit">Save Burger!</button>
</form>

<script type="text/javascript">
  $(".deldevoured").on("click", function(event) {
    // Get the ID from the button.
    // This is shorthand for $(this).attr("data-devouredid")
    let id = $(this).data("devouredid");

    // Send the DELETE request.
    $.ajax("/api/burgers/" + id, {
      type: "DELETE"
    }).then(
      function() {
        console.log("deleted id ", id);
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });

 $("#createburger_name").on("submit", function(event) {
    // Make sure to preventDefault on a submit event.
    event.preventDefault();

    // [name=burger_name] will find an element with a "name" attribute equal to the string "burger_name"
    let newBurger = {
      burger_name: $("#createburger_name [name=burger_name]").val().trim()
    };
    console.log(newBurger.burger_name);
    // Send the POST request.
    $.ajax( {
      url: "/api/burgers",
      type: "POST",
      data: {
        newBurger: burger_name
      }
    }).then(
      function() {
        console.log("created new burger");
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });



   
</script>

